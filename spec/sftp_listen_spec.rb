require 'spec_helper'

describe SftpListen do
  it 'has a version number' do
    expect(SftpListen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
